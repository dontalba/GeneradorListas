services:
  - type: web
    name: GeneradorListas
    env: dotnet
    plan: free
    buildCommand: dotnet publish -c Release -o out
    startCommand: dotnet out/GeneradorListas.dll.dll
    branch: main
    rootDir: .
